# Runtime dependencies (production).
# Dev/test tools live in requirements-dev.txt.
python-telegram-bot[job-queue]==22.8
aiosqlite==0.22.1
matplotlib==3.11.0
# bot/charts.py imports numpy directly, so declare it rather than relying on it
# arriving transitively via matplotlib.
numpy==2.5.1
python-dotenv==1.2.2
PyYAML==6.0.2
tzdata==2026.3
# Used by bot/services/llm_parser.py for the optional Gemini call. It already
# arrives transitively via python-telegram-bot, but declare it rather than
# depending on a transitive pin (same reasoning as numpy above).
httpx==0.28.1
